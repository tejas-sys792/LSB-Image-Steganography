# LSB-Image-Steganography
Image Steganography in C  A C program that hides secret text files inside BMP images using LSB (Least Significant Bit) encoding. Supports encoding and decoding, optional custom magic strings, automatic file extension handling, and robust file validation and capacity checks.  Features:  Encode/decode secret messages into BMP images 
